# Gmail API Configuration
# Copy this file to .env and fill in your actual values

# Google OAuth2 credentials
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_client_secret_here

# OAuth2 redirect URI (usually http://localhost:8080 for local development)
GOOGLE_REDIRECT_URI=http://localhost:8080

# Scopes needed for Gmail access
GOOGLE_SCOPES=https://www.googleapis.com/auth/gmail.readonly

# Note: token.json will be generated automatically after first authorization
# Do NOT commit token.json or client_secret*.json files to Git!
